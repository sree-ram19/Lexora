# Lexora Backend — pinned dependencies
# Install: pip install -r requirements.txt

# Core ML / Embeddings
sentence-transformers==3.0.1
torch==2.3.1

# Reranking
# cross-encoder is included in sentence-transformers

# BM25
rank-bm25==0.2.2

# Vector DB
chromadb==0.5.3

# LLM
groq==0.9.0

# PDF Processing
PyMuPDF==1.24.5          # fitz — text + image extraction from PDF
pymupdf4llm==0.0.5       # markdown-aware extraction (bonus)

# OCR
pytesseract==0.3.13
easyocr==1.7.1
Pillow==10.4.0

# NLP / Chunking
nltk==3.8.1

# Diagram / Chart generation
matplotlib==3.9.0
plotly==5.22.0
kaleido==0.2.1            # plotly static export

# Env / Utils
python-dotenv==1.0.1
tenacity==8.3.0           # retry logic
tqdm==4.66.4
numpy==1.26.4
